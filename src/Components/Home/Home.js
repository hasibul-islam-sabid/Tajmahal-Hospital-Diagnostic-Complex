import React from 'react';
import NavBar from '../NavBar/NavBar';
import THCarousel from './THCarousel/THCarousel';

const Home = () => {
    return (
        <div>
            <THCarousel />
            <NavBar />
        </div>
    );
};

export default Home;